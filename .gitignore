node_modules/
dist/
build/
.turbo/
coverage/
*.log
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

playwright-report/
test-results/

# Documents personnels utilisés comme fixtures locales — jamais versionnés (à toute profondeur)
**/fixtures/private/

# Cache de dépendances Vite
.vite/

# Fichiers stockes sur disque (CV importes) — jamais versionnes
# Ancre a la racine : sans le `/` initial, ce motif masquerait aussi
# apps/api/src/common/storage/ (code source, pas des fichiers uploades).
/storage/

# Configuration locale de l outil (launch.json)
.claude/
